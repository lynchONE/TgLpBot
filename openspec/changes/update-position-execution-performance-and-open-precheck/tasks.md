## 1. Implementation
- [ ] 1.1 新增 `open_position_prepare` 后端接口与短时上下文复用能力，支持界面打开即发起池子预检测。
- [ ] 1.2 调整 `open_position_preview` 和 `open_position`，优先复用 prepare 阶段结果，并保留旧客户端兼容回退。
- [ ] 1.3 拆分开仓与撤仓关键路径，把交易记录、交易历史、补账和非关键通知改为异步补写与重试。
- [ ] 1.4 将开仓固定等待、allowance 固定重查、撤仓 receipt/余额同步等待改为 BSC 优先的快路径轮询加保守回退。
- [ ] 1.5 将撤仓失败后的任务调度改为 `500ms -> 1s -> 2s -> 3s -> 5s -> 10s -> 30s` 的梯度退避，并补充日志字段。
- [ ] 1.6 接入 WebApp 与 MiniApp，在开仓界面打开时立即执行预检测，并在金额/滑点变化时做增量预览。
- [ ] 1.7 补充回归测试与日志验证，覆盖快路径命中、回退路径、异步补写失败、梯度退避、旧客户端兼容五类场景。
