## 1. Backend
- [x] 1.1 定义功能模块目录和合法 key，覆盖 `webapp` 与 `miniapp` 当前用户可见功能模块。
- [x] 1.2 扩展 `UserAccess` 与 `AuthCode` 的模块授权持久化结构，包含旧数据迁移或启动补齐策略。
- [x] 1.3 扩展授权服务：创建/更新用户授权、创建/更新授权码、兑换授权码时读写模块清单，并拒绝未知模块 key。
- [x] 1.4 扩展管理员授权 API 请求和响应，返回模块目录、用户已授权模块、授权码默认模块和授权摘要。
- [x] 1.5 在关键用户 API 增加模块权限校验，确保入口隐藏之外仍有后端边界。
- [x] 1.6 补充后端单测，覆盖模块保存、字段缺失不覆盖、空数组清空、授权码兑换继承模块、非授权模块 API 拒绝。

## 2. Frontend
- [x] 2.1 在 `webapp` 与 `miniapp` API 客户端接入模块授权字段和模块目录。
- [x] 2.2 集中整理前端功能模块元数据，用于导航过滤、授权矩阵和状态展示。
- [x] 2.3 重设计 `webapp` 管理员授权工作台，提供列表-详情布局、模块分组勾选、全部授权、清空、保存和危险操作确认。
- [x] 2.4 重设计 `miniapp` 管理员授权工作台，保持同等能力并适配移动端单列操作。
- [x] 2.5 根据用户模块授权过滤 `webapp` 工作台模块和 `miniapp` 底部/页面入口；未授权直达页面时显示明确的无权限状态。
- [x] 2.6 检查文案、按钮、输入控件和响应式布局，确保管理员模块简洁、美观、易操作。

## 3. Verification
- [x] 3.1 运行 `cd backend; go test ./...`。
- [x] 3.2 运行 `cd webapp; npm run build`。
- [x] 3.3 运行 `cd miniapp; npm run build`。
- [x] 3.4 做针对性 diff 检查，确认模块 key、API 字段、授权码兑换和 UI 状态一致，没有引入不必要 fallback。
- [x] 3.5 尝试运行 OpenSpec 严格校验；当前环境未安装 `openspec` CLI，无法执行。
