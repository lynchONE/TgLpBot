## 1. Implementation
- [x] 1.1 增加 SoSoValue API Key、同步间隔、月度安全阈值等配置项。
- [x] 1.2 新增新闻数据模型和月度请求用量模型，并加入 AutoMigrate。
- [x] 1.3 实现 SoSoValue 新闻同步服务：请求、归一化、入库、去重和 24 小时清理。
- [x] 1.4 实现月度请求计数与阈值保护，避免超过 100,000 次/月限制。
- [x] 1.5 新增 WebApp 新闻读取接口，优先返回数据库内 24 小时缓存。
- [x] 1.6 在 WebApp API client 中增加新闻请求方法。
- [x] 1.7 在首页顶部新增推荐新闻组件，放入用户标注的顶部空白区域。
- [x] 1.8 在页面最底部新增新闻 ticker 横向滚动条。
- [x] 1.9 补充样式，确保顶部推荐新闻、底部 ticker 不遮挡现有登录区、布局控制区和工作台模块。

## 2. Validation
- [x] 2.1 运行 `gofmt` 检查 Go 文件。
- [x] 2.2 运行相关 Go 测试或至少编译相关 package。
- [x] 2.3 运行 `npm run build`。
- [x] 2.4 做 targeted diff 检查，确认 API 字段、调用方、配额保护和样式范围匹配。
