## 1. Schema / Migration
- [x] 1.1 为自动跟单配置增加目标钱包数组、触发模式、阈值钱包数和窗口字段。
- [x] 1.2 将已有单钱包配置回填为单元素钱包数组，并保持 `target_wallet_address` 兼容。
- [x] 1.3 为跟单作业增加触发钱包列表和触发事件列表字段。

## 2. Backend
- [x] 2.1 扩展保存配置请求与输入校验，支持多个合法 EVM 地址并去重。
- [x] 2.2 实现 `any` 模式：任一目标钱包新开仓事件创建跟单作业。
- [x] 2.3 实现 `threshold` 模式：同池子、同协议、同 tick 区间在窗口内达到 N 个不同钱包开仓才创建跟单作业。
- [x] 2.4 目标钱包组、触发模式或阈值变更时，将游标推进到当前最新事件，避免历史回放。
- [x] 2.5 保持撤仓跟单只作用于该配置创建的跟单仓位。

## 3. Frontend
- [x] 3.1 WebApp 自动跟单表单支持添加、删除、校验多个目标钱包地址。
- [x] 3.2 MiniApp 自动跟单表单支持添加、删除、校验多个目标钱包地址。
- [x] 3.3 配置卡片展示钱包数量、触发模式、阈值和窗口。
- [x] 3.4 最近任务展示触发钱包列表或触发钱包数量。

## 4. Verification
- [x] 4.1 增加后端单元测试覆盖地址组校验、阈值越界和阈值仓位引用。
- [x] 4.2 增加可重复迁移检查，确认旧 `target_wallet_address` 配置可正常升级。
- [x] 4.3 执行针对性 Go 测试和前端 build。
- [x] 4.4 做针对性 diff 检查，确认没有引入隐藏 fallback 或默认钱包地址。
