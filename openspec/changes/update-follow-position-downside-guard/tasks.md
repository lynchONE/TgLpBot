## 1. 后端策略
- [x] 1.1 调整 `StrategyService` 出区间逻辑：`is_follow=true` 的任务在下破区间时立即执行撤出并停止任务。
- [x] 1.2 保持跟单任务上破区间不自动撤出、不自动再平衡。
- [x] 1.3 确保下破保底复用现有撤仓到 USDT 与任务状态/错误处理路径。
- [x] 1.4 补充单元测试覆盖跟单下破触发撤出、上破跳过、普通任务行为不变。

## 2. 仓位展示
- [x] 2.1 后端实时仓位数据为跟单仓位提供足够信息，让前端能展示跟单策略说明。
- [x] 2.2 WebApp 仓位卡片对跟单仓位展示“目标撤仓跟随 / 下破保底撤出 / 上破继续跟随”。
- [x] 2.3 MiniApp 仓位卡片对跟单仓位展示同等说明，并避免把跟单仓位误标为普通“双向撤出”策略。

## 3. 验证
- [x] 3.1 运行后端相关测试。
- [x] 3.2 运行 WebApp/MiniApp 构建或至少 MiniApp 构建。
- [x] 3.3 做 targeted diff 检查，确认 API 字段、调用和 UI 文案一致。
