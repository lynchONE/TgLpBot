## 1. Schema / Migration
- [x] 1.1 为 `smart_money_follow_configs` 增加任务名称字段，并确保迁移可重复执行。

## 2. Backend
- [x] 2.1 扩展自动跟单保存请求与 `SaveConfigInput`，接收任务名称。
- [x] 2.2 规范化任务名称：去除首尾空白，空字符串保存为空，超长名称拒绝保存。
- [x] 2.3 列表接口返回任务名称，编辑已有配置时保留该字段。
- [x] 2.4 增加后端测试覆盖任务名称规范化、清空和超长拒绝。

## 3. Frontend
- [x] 3.1 WebApp 自动跟单配置表单增加任务名称输入，并在配置卡片/删除确认/任务流中展示。
- [x] 3.2 MiniApp 自动跟单配置表单增加任务名称输入，并在配置卡片/任务流中展示。
- [x] 3.3 未填写任务名称时继续使用现有目标钱包展示，不引入误导性默认名称。

## 4. Verification
- [x] 4.1 执行相关 Go 测试。
- [x] 4.2 执行 WebApp 和 MiniApp 构建。
- [x] 4.3 做针对性 diff 检查，确认 API 字段、表单状态和展示调用匹配。
