# Tasks

- [x] 1. 后端候选查询
  - [x] 1.1 增加查询最近 N 天无 LP add/remove 活动的 active 聪明钱钱包方法，默认 N=30。
  - [x] 1.2 返回钱包地址、chain_id、标签、来源、最后 LP 活动时间、窗口内 add/remove 次数、历史事件/仓位/快照计数等确认字段。
  - [x] 1.3 复用现有钱包视图权限和链选择逻辑。

- [x] 2. 后端批量删除
  - [x] 2.1 增加批量删除接口，入参为用户确认选择的钱包 address + chain_id 列表。
  - [x] 2.2 删除目标钱包的 `monitored_wallets`、LP 事件、仓位、active state、转账事件、每日快照、live state、LP 日统计、特别关注和跟单引用数据。
  - [x] 2.3 删除成功后失效聪明钱钱包列表、排行榜或相关缓存。
  - [x] 2.4 将现有单个钱包删除统一改为相同硬删除语义。

- [x] 3. 钱包视图交互
  - [x] 3.1 在钱包视图增加“查找僵尸钱包”按钮。
  - [x] 3.2 用弹窗展示候选列表，默认全选，并支持逐个取消选择。
  - [x] 3.3 提交前展示将删除的钱包数量和历史数据删除提示，删除后刷新钱包列表。

- [x] 4. 验证
  - [x] 4.1 增加后端候选查询与批量硬删除单元测试。
  - [x] 4.2 增加前端 API 封装与关键交互的构建验证。
  - [x] 4.3 运行 `go test ./...` 和对应前端 build。
