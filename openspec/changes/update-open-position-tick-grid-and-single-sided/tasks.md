## 1. 规格与语义
- [x] 1.1 补充 OpenSpec，明确价格输入属于前端编辑能力，不新增后端 `price` 模式
- [x] 1.2 明确单边快捷按钮的行为：保留当前宽度，将区间整体移动到当前价下方或上方

## 2. WebApp
- [x] 2.1 为开仓弹窗新增“价格区间”编辑模式，并将价格输入映射为合法 Tick
- [x] 2.2 新增 `单边下限` / `单边上限` 快捷按钮
- [x] 2.3 将单边/双边结果升级为更显式的摘要层，并与图表、Tick、格子编辑保持联动

## 3. MiniApp
- [x] 3.1 为开仓面板新增“价格区间”编辑模式，并将价格输入映射为合法 Tick
- [x] 3.2 新增 `单边下限` / `单边上限` 快捷按钮
- [x] 3.3 将单边/双边结果升级为更显式的摘要层，并与图表、Tick、格子编辑保持联动

## 4. 验证
- [x] 4.1 运行 WebApp 构建验证
- [x] 4.2 运行 MiniApp 构建验证
- [ ] 4.3 手动验证价格输入、单边下限、单边上限、图表拖拽与点柱选择
