## 1. Implementation
- [x] 1.1 后端：放开钱包单币兑换对原生币伪地址的限制，并保持 OKX 请求参数为官方要求的 `0xeeee...`
- [x] 1.2 后端：让 OKX 执行器支持原生币 `tx.value`、跳过 approve、按 native balance 计算到账
- [x] 1.3 后端：钱包余额预览返回的原生币标记为可兑换
- [x] 1.4 前端：在常用代币中提供原生 `BNB/ETH` 入口，并允许直接选择原生币
- [x] 1.5 测试：补充原生币参数与余额增量回归测试
- [x] 1.6 验证：运行 `go test` 与 `webapp` 构建
