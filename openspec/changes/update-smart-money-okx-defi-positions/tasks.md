## 1. OpenSpec
- [x] 1.1 定义 OKX DeFi 聪明钱钱包/持仓展示的 WebApp、MiniApp 与性能要求。
- [x] 1.2 校验本变更 OpenSpec 结构；本机无 `openspec` 命令，已用标题与场景结构搜索做自查，无法执行工具校验。

## 2. Backend
- [x] 2.1 在 OKX 客户端中新增 DeFi 用户资产平台列表请求，复用现有鉴权、超时和 debug 日志策略。
- [x] 2.2 在 OKX 客户端中新增 DeFi 用户资产平台详情请求，支持按钱包、平台、链等必要参数查询。
- [x] 2.3 为 OKX DeFi 响应定义规范化结构，至少输出链维度金额、平台金额、投资品、position、手续费、仓位金额和区间字段。
- [x] 2.4 新增聪明钱钱包 OKX DeFi 概览接口/响应字段，并在钱包详情页数据路径中按需返回。
- [x] 2.5 新增聪明钱 OKX DeFi 平台/持仓详情接口，点击单个持仓时按需加载详情。
- [x] 2.6 特殊关注钱包接口复用 OKX DeFi 概览聚合，避免重复实现。
- [x] 2.7 增加短 TTL 缓存、请求超时和错误状态透传；不得把 OKX 失败静默显示为 0 仓位。

## 3. WebApp
- [x] 3.1 钱包详情页展示 OKX DeFi 各链数据、平台持仓金额和更新时间/状态。
- [x] 3.2 特殊关注钱包展示 OKX DeFi 总额和链维度概览。
- [x] 3.3 点击某个 OKX DeFi 持仓时展示详情，突出手续费、仓位金额和区间。
- [x] 3.4 保留现有本地 LP 仓位详情入口，不用 OKX 展示替换已有实时仓位卡片。

## 4. MiniApp
- [x] 4.1 钱包详情页展示 OKX DeFi 各链数据、平台持仓金额和更新时间/状态。
- [x] 4.2 特殊关注钱包展示 OKX DeFi 总额和链维度概览。
- [x] 4.3 点击某个 OKX DeFi 持仓时展示详情，突出手续费、仓位金额和区间。

## 5. Verification
- [x] 5.1 后端新增/更新单元测试覆盖 OKX DeFi 客户端、handler 参数校验和响应 shape。
- [x] 5.2 运行 `cd backend; go test ./...`。
- [x] 5.3 运行 `cd webapp; npm run build`。
- [x] 5.4 运行 `cd miniapp; npm run build`。
- [x] 5.5 做针对性 diff 检查，确认没有修改交易执行、授权、nonce、开仓、撤仓逻辑。
