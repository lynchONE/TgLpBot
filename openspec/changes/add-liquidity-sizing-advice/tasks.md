## 1. Implementation
- [ ] 1.1 新增仓位规模建议计算模块，封装目标占比反推、80% 截断、风险上限与总资金上限处理。
- [ ] 1.2 新增自动输入解析逻辑，从开仓上下文中获取当前价格/current tick、用户区间 ticks、`L_active`、当前钱包 stable 资金规模与默认风险参数。
- [ ] 1.3 扩展开仓预检响应，在不改变现有开仓请求参数的前提下返回建议 JSON。
- [ ] 1.4 新增默认配置读取逻辑，用于解析目标占比范围和风险约束；优先用户全局配置，缺省回退系统配置。
- [ ] 1.5 在返回结果中补充 `warnings` 与可解释的计算摘要，明确标记低效率区间、资金/风险截断、自动回退来源和档位收敛。
- [ ] 1.6 为核心计算补充单元测试，覆盖：
- [ ] 1.7 目标占比超过 `0.8` 的截断与低效率警告
- [ ] 1.8 固定金额上限与比例上限并存时的有效风险上限计算
- [ ] 1.9 推荐金额被风险上限和总资金上限截断后的 `expected_share` 重算
- [ ] 1.10 池子流动性和钱包资金读取失败时的回退与降级输出
- [ ] 1.11 窄目标区间导致三档结果重叠的输出稳定性
