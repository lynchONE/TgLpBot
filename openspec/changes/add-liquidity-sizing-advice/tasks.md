## 1. Implementation
- [x] 1.1 新增仓位规模建议计算模块，封装目标占比反推、80% 截断和档位收敛处理。
- [x] 1.2 新增自动输入解析逻辑，从开仓上下文中获取当前价格 / current tick、用户区间 ticks、`L_active` 与默认目标占比参数。
- [x] 1.3 扩展开仓预检响应，在不改变现有开仓请求参数的前提下返回建议 JSON。
- [x] 1.4 新增默认配置读取逻辑，用于解析目标占比范围；优先用户全局配置，缺省回退系统配置。
- [x] 1.5 在返回结果中补充 `warnings` 与可解释的计算摘要，明确标记低效率区间、自动回退来源和档位收敛。
- [x] 1.6 为核心计算补充单元测试，覆盖：
- [x] 1.7 目标占比超过 `0.8` 的截断与低效率警告。
- [x] 1.8 三档推荐不再依赖钱包余额或总资金上限。
- [x] 1.9 `expected_share` 与理论推荐金额保持一致。
- [x] 1.10 池子流动性读取失败时的回退与降级输出。
- [x] 1.11 窄目标区间导致三档结果重叠时的输出稳定性。
