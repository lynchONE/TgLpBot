## 1. Backend
- [x] 1.1 增加按用户特别关注钱包查询 LP 事件的读取方法，过滤当前用户、链、钱包集合和 `add/remove` 事件。
- [x] 1.2 暴露 MiniApp 可调用接口，校验 Telegram `initData`，返回分页列表、总数和必要展示字段。
- [x] 1.3 补充后端测试，覆盖无关注钱包、只返回当前用户关注钱包、只返回 add/remove 事件和分页。

## 2. MiniApp
- [x] 2.1 在 `smartMoneyApi` 中新增特别关注动态接口封装。
- [x] 2.2 在聪明钱主导航新增“特别关注”模块入口，保持现有“特别关注”按钮和提醒配置不变。
- [x] 2.3 展示特别关注钱包列表，点击钱包后显示该钱包最近 LP 加仓/撤仓记录。
- [x] 2.4 增加空状态、加载状态、错误状态和分页或加载更多。

## 3. WebApp
- [x] 3.1 在 `smartMoneyApi` 中新增特别关注动态接口封装。
- [x] 3.2 在聪明钱主导航新增“特别关注”模块入口，保持现有“特别关注”心形按钮和提醒配置不变。
- [x] 3.3 展示特别关注钱包列表，点击钱包后显示该钱包最近 LP 加仓/撤仓记录。
- [x] 3.4 增加空状态、加载状态、错误状态和分页或加载更多。

## 4. Verification
- [x] 4.1 执行针对性 diff 检查，确认没有影响池子视图、钱包视图和监控通知原有行为。
- [x] 4.2 运行可用的后端测试、`miniapp` 构建和 `webapp` 构建。
