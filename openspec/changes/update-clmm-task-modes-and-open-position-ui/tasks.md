## 1. Implementation
- [ ] 1.1 Backend：为 `strategy_tasks` 增加 `out_of_range_mode` 字段，定义默认值与旧字段兼容映射。
- [ ] 1.2 Backend：开仓接口支持 `task_mode`，默认值改为 `exit_all`，并支持以暂停态创建任务。
- [ ] 1.3 Backend：新增任务模式更新接口，旧 `toggle_rebalance` 继续兼容两种基础模式。
- [ ] 1.4 Backend：重写越界策略分发逻辑，支持双向再平衡、双向撤出、上破再平衡下破撤出三种模式，并保持暂停态优先跳过。
- [ ] 1.5 Backend：实时仓位响应补充当前模式字段/展示文案，供前端仓位卡渲染。
- [ ] 1.6 WebApp：开仓弹窗默认模式改为“再平衡关闭”，移除三档建议展示，压缩前置兑换展示并去掉额外确认复选框。
- [ ] 1.7 MiniApp：开仓页同步 WebApp 的模式按钮、默认值、前置兑换展示与三档建议移除。
- [ ] 1.8 WebApp / MiniApp：仓位卡改为任务模式按钮，支持切换模式、暂停和恢复。
- [ ] 1.9 Tests：补充后端单测，覆盖模式映射、非对称越界分支、暂停态优先级、旧接口兼容行为。
- [ ] 1.10 Verification：运行 `go test ./...`、前端构建命令，并检查模式切换与开仓提交流程。
