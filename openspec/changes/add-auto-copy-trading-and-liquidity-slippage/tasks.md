## 1. OpenSpec
- [x] 1.1 确认本变更与 `add-miniapp-smart-money-follow-wallet`、`refactor-smart-money-position-detail-read-model`、`update-open-slippage-and-task-range` 无冲突。
- [x] 1.2 提案通过后再进入实现。

## 2. 补充流动性滑点
- [x] 2.1 后端 `task_add_liquidity` 请求增加可选 `slippage_tolerance`，并复用现有滑点校验。
- [x] 2.2 `IncreaseLiquidityForTask` 或其参数结构支持本次滑点覆写，未传时使用任务当前滑点。
- [x] 2.3 MiniApp / WebApp 补流动性弹窗展示当前仓位滑点作为默认值，并允许用户修改。
- [x] 2.4 增加默认滑点、覆写滑点和非法滑点的后端测试。

## 3. 自动跟单后端
- [x] 3.1 新增自动跟单配置、作业和任务映射模型，并纳入迁移。
- [x] 3.2 新增自动跟单配置列表、创建/更新、启停、删除接口，要求 Telegram WebApp 认证与聪明钱权限。
- [x] 3.3 实现从 `sm_lp_events` 读取目标钱包新事件并生成去重作业的 worker。
- [x] 3.4 实现固定金额与比例金额的开仓跟单执行。
- [x] 3.5 实现撤仓跟单开关与仅撤出自动跟单任务的隔离逻辑。
- [x] 3.6 记录作业状态、失败原因、关联事件和关联任务，供前端展示。
- [x] 3.7 增加配置校验、作业去重、金额计算、延时调度、撤仓隔离的单元测试。

## 4. 自动跟单前端
- [x] 4.1 聪明钱模块新增“自动跟单”页签。
- [x] 4.2 新增配置列表、创建/编辑表单和启停操作。
- [x] 4.3 表单支持目标钱包、金额模式、固定金额、比例、立即/延时、撤仓是否跟单。
- [x] 4.4 展示最近跟单作业状态与错误原因。
- [x] 4.5 执行 `npm run build` 验证前端构建。

## 5. Verification
- [x] 5.1 执行针对性 Go 测试。
- [x] 5.2 执行前端构建。
- [x] 5.3 修改后做针对性 diff 检查，确认 API 字段、调用点和回归风险。
