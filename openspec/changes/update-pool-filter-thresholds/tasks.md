## 1. 实现
- [x] 1.1 热门池子筛选状态增加最大费率阈值，并持久化到现有本地筛选配置。
- [x] 1.2 热门池子筛选弹层增加“排除费率高于 (%)”输入，应用后过滤 `fee_rate > maxFeeRate` 的池子。
- [x] 1.3 聪明钱池子视图增加筛选按钮、弹层和筛选状态。
- [x] 1.4 聪明钱池子筛选支持最低当前聪明钱仓位金额和最高池子费率。
- [x] 1.5 补充必要的空值/清空/重置逻辑，确保默认行为不变。

## 2. 验证
- [x] 2.1 `cd backend && go test ./...`
- [ ] 2.2 `cd webapp && npm run build`
- [ ] 2.3 如 MiniApp 涉及改动，运行 `cd miniapp && npm run build`
